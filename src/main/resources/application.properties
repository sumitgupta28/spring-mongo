# Configuration migrated to application.yml
# The MongoDB and other application settings are now stored in application.yml
# Keep this file only for reference or delete it.
# application.yml contains:
# spring:
#   data:
#     mongodb:
#       uri: mongodb://localhost:27017/springdb
#       database: springdb

